# MarketPulse

Перед запуском проекта убедитесь, что [docker](https://docs.docker.com/desktop/setup/install/linux/) уже установлен на вашей машине🤝

## Общие сведения о проекте

### Цель проекта

Цель проекта MarketPulse — создание цифровой платформы, обеспечивающей удобную коммуникацию между продавцами и пользователями путем предоставления инструментов для размещения товаров и услуг продавцами, формирования персонализированных корзин покупок пользователями. Платформа направлена на повышение удобства взаимодействия покупателей с различными поставщиками, улучшение качества обслуживания и увеличение объема продаж за счёт оптимизации процесса покупки и коммуникации.

### Выполняемые задачи

1. Разработка функциональной и удобной платформы
2. Обеспечение постоянной доступности и стабильности системы
3. Автоматизация процессов разработки и развертывания (CI/CD)
4. Универсальность решения для разных операционных систем (ОС)

## Функциональность продукта

### Для Продавцов

1. Возможность добавления новых товаров/услуг с указанием названия, описания, цены и загрузки основного изображения
2. Редактирование данных о существующих товарах/услугах
Просмотр полного списка заказов и детализированной информации по каждому заказу
3. Доступ к списку своих клиентов с подробной информацией о каждом клиентском заказе
4. Отображение истории заказов каждого клиента исключительно по собственным товарам/услугам

### Для Покупателей

1. Поиск товаров/услуг по множеству критериев фильтрации (название, цена)
2. Общий доступ ко всему ассортименту товаров/услуг и просмотр полной информации о продукте
3. Оформление заказа с указанием деталей доставки.
Добавление и удаление количества товаров в корзину, изменение общего состава корзины
4. Просмотр содержимого корзины с отображением общей стоимости выбранных товаров
5. Личный кабинет покупателя включает возможность просмотра всей истории заказов и подробную информацию по каждому ранее совершенному заказу, сегментированную по отдельным продавцам

## Этапы развертывания проекта

### Этап 1. Подготовка инфраструктуры

1. Для запуска сервисов потребуется выбрать платформу (облачные сервисы, собственные сервера).
Далее для демонстрации работы используется облачный сервис [Yandex.Cloud](https://yandex.cloud/ru)
2. Выбрать и произвести настройку базы данных, хранилищ данных.
В проекте используется локальная база данных [PostgreSQL](https://www.postgresql.org/)
3. По умолчанию [docker](https://docs.docker.com/desktop/setup/install/linux/) уже установлен на вашей платформе для развертывания.

### Этап 2. Возможности и взаимодействие API

#### Основные функциональные возможности API для продавцов

1. Создание и управление товарами/услугами:
    1. продавцы могут добавлять новые товары и услуги, указывая такие характеристики, как название, описание, цена и загрузка изображений. Эти операции осуществляются через `POST /products`, `PUT /products/{id}`. Данные сохраняются в базу данных и становятся доступными для пользователей
2. Редактирование данных о товарах/услугах:
    1. через API-продавцы могут вносить изменения в существующие записи товаров/услуг (редактировать название, описание, цену, заменять изображения). Это реализуется методом `PUT` или `POST` запросов к соответствующим ресурсам `PUT /products/{id}`, `POST /products/{id}`
3. Просмотр заказов и информации о клиентах:
    1. продавцы получают доступ к полному списку заказов и могут детально изучить каждый заказ, увидеть состав корзины, сумму заказа и контактную информацию покупателя. Эта информация доступна через GET-запросы `GET /orders` и `GET /orders/{order_id}`
4. История заказов и профиль клиентов:
    1. продавцы могут видеть всю историю заказов по каждому клиенту именно по своим товарам/услугам. Они получают подробную информацию обо всех сделанных клиентом заказах через `GET /customers/{customer_id}/orders`

#### Основные функциональные возможности API для покупателей

1. Поиск товаров/услуг:
    1. пользователи могут осуществлять поиск товаров/услуг по различным критериям (название, категория, диапазон цен и т.п.). Этот функционал доступен через API `GET /search?q={query}&price_from={min_price}&price_to={max_price}`

2. Просмотр ассортимента и карточек товаров:
    1. покупатели могут просматривать полный перечень товаров и услуг, а также детальную информацию о конкретном товаре через соответствующие запросы `GET /products`, `GET /products/{product_id}`

3. Оформление заказа:
    1. после выбора нужных товаров покупатели могут оформить заказ, заполнив необходимые контактные и адресные данные для доставки. Процедура оформления заказа выполняется через POST-запросы к ресурсу заказов `POST /orders`

4. Управление корзиной:
    1. клиент имеет возможность добавить товары в корзину, удалить отдельные позиции или изменить их количество. Все манипуляции происходят через интерфейс `POST /cart/add`, `DELETE /cart/remove`, `PUT /cart/update-quantity`

5. Отображение итогового счёта и содержание корзины:
    1. посредством API покупатель получает доступ к содержанию своей корзины и общей сумме выбранного товара перед оформлением заказа `GET /cart`

6. Личная история заказов:
    1. у пользователей есть доступ к своему профилю, где хранится вся история предыдущих заказов, полная информация о каждом из них и подробные сведения о проданном товаре `GET /users/{user_id}/orders`

#### Архитектурные особенности взаимодействия API

1. Вся работа с системой построена на REST архитектуре с использованием HTTP-методов (`GET`, `POST`, `PUT`, `DELETE`), а также JSON формата передачи данных

2. Авторизация и аутентификация обеспечиваются с использованием токенов JWT, что гарантирует безопасность доступа и конфиденциальность данных

3. Каждый продавец работает с собственными ресурсами, ограниченными кругом своих товаров и заказов, исключая несанкционированный доступ к данным других участников рынка

### Этап 3. Тестирование

#### Тестирование backend-части

#### Используемые технологии

Для проведения комплексного тестирования использовались следующие технологии:

1. **Django Unittest Framework**
Базовый фреймворк для создания юнит-тестов.
2. **Django REST Framework**
Фреймворк, предоставляющий инструменты для тестирования API.
3. **Model Bakery**
Библиотека, предназначенная для удобного создания тестовых данных.
4. **Coverage**
Инструмент, позволяющий измерить покрытие кода тестами.

Использование **Model Bakery** позволяет создавать тестовые данные без дублирования кода, автоматически заполнять необязательные поля.

Для удобства тесты расположены в отдельной директории `tests`:

```directory
tests/
├── models/
│ ├── test_brand.py
│ ├── ...
│ └── test_cart.py
├── api_view/
│ ├── test_category.py
│ ├── ...
│ └── test_product.py
├── html_view/
│ ├── test_main.py
│ ├── ...
│ └── test_brand.py
├── integration/
│ ├── test_brand.py
│ └── test_product.py
├── init.py
└── Bakery.py
```

Такая организация позволяет:

- Удобно разделять тесты по типам и назначению
- Запускать тесты выборочно
- Масштабировать тестовую базу по мере развития приложения

#### Виды тестирования

#### Юнит-тестирование

1. **Models (`tests.models`)**
    Включает в себя:
   - Проверку всех полей моделей
   - Проверку ограничений базы данных (unique, null, и т.д.)
   - Тестирование связей между моделями
   - Тестирование методов

2. **API_views (`tests.api_view`)**
    Включает в себя:
   - Тестирование CRUD-операций
   - Проверку валидации данных
   - Проверку HTTP-кодов ответов
   - Тестирование URL-маршрутов

3. **HTML_views (`tests.html_view`)**
    Включает в себя:
    - Проверку шаблонов
    - Тестирование URL-маршрутов
    - Проверку контекста

#### Интеграционное тестирование (`tests.integration`)

   Включает в себя:

- Проверку работы цепочек API-запросов
- Взаимодействия моделей между собой
- Тестирование API эндпоинтов

### Этап 4. Запуск и поддержка

#### Подготовки платформы(сервера)

1. Войдите в собственный аккаунт github
2. Скопируйте репозиторий к себе в github
3. Перейдите по следующему пути в полученном репозитории:
```Settings ->  Actions -> Runners -> New self-hosted runner```
Выберете операционную систему, которая установлена на сервере и следуйте ниже перечисленным инструкциям. Действия необходимо выполнить на той платформе, где ожидается развертывание всей системы
4. После успешного запуска [runner](https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners/about-self-hosted-runners) требуется подготовить [docker swarm](https://docs.docker.com/engine/swarm/):
    1. откройте новую консоль и введите команду по первоначальному созданию [manager](https://docs.docker.com/engine/swarm/admin_guide/) node: `sudo docker swarm init`
    2. сохраните полученный токен для возможности в будущем подключить [worker](https://docs.docker.com/engine/swarm/admin_guide/) node

После выполнения действий описанных выше сервер готов принимать с репозитория github изменения и автоматически разворачивать систему на выбранной платформе. Для тестирования работоспособности выполненных действий можно выполнить commit в ветке main для файла [README.md](./README.md)

#### Организация постоянного мониторинга работоспособности

1. В разработанной системе предусмотрен мониторинг активности системы с помощью [Grafana](https://grafana.com/). В Grafana можно настроить отправку уведомлений на электронную почту при достижении определённых показателей метрик (данное действие нужно настраивать ручным способом).
2. Перед обновлением контейнеров на основной платформе выполняется проверка работоспособности системы:
    1. `build` — получает актуальные изменения из репозитория и пытается собрать всю систему на сервере github. Если успешно(зеленая галочка), то переходит к следующему шагу, иначе будет получено письмо на электронную почту github
    2. `test` — получает актуальные изменения из репозитория и пытается запустить прописанные [тесты](#этап-3-тестирование). Если успешно(зеленая галочка), то переходит к следующему шагу, иначе будет получено письмо на электронную почту github
    3. `deploy` — получает актуальные изменения из репозитория и пытается собрать всю систему на выбранной платформе. Если успешно(зеленая галочка), то docker swarm автоматически шаг за шагом будет подменять старые контейнеры на новые в бесшовном режиме, иначе будет получено письмо на электронную почту github и docker swarm не будет подменять работающие контейнеры.
    Если возникнет проблема с новыми сборками при попытке замены контейнеров, то docker swarm автоматически запустит предыдущую рабочую версию контейнера
3. Добавлена отказоустойчивость системы, если в какой-то момент контейнер(ы) упадет(ут) по ошибке, то docker swarm попытается автоматически восстановить по правилам описанным в файле [docker-compose.yml](./docker-compose.yml)
4. Внедрен [pgAdmin](https://www.pgadmin.org/) для проверки работоспособности базы данных и оперативного изменения данных в обход backend и frontend

#### Описание файлов конфигурации системы

1. [docker-compose.yml](./docker-compose.yml) — содержит инструкции по описанию действий для развертывания всей системы и описание поведения  в случае возникновения ошибок при сборке или работе контейнера
2. [.gitignore](./.gitignore) — файл содержащий исключения на добавления в удаленный репозиторий GitHub
3. [datasources.yaml](./grafana/datasources/datasources.yaml) — инструкции по приему данных из источника для Grafana
4. [dashboards.yaml](./grafana/dashboards/dashboards.yaml) — инструкции по отображению данных из источников для Grafana
5. [node-exporter](./grafana/dashboards/node-exporter/1860_rev37.json) — источник данных для Prometheus
6. [vite.config.js](./frontend/vite.config.js) — инструкции для запуска frontend
7. [frontend/Dockerfile](./frontend/Dockerfile) — сборка контейнера frontend
8. [servers.json](./backend/scripts/pg-admin-scripts/servers.json) — файл настройки автоматического подключения к базе данных для pgAdmin
9. [prometheus.yml](./backend/scripts/prometheus-scripts/prometheus.yml) — инструкции по регистрации источников данных для Prometheus
10. [backend/Dockerfile](./backend/lyalya/Dockerfile) — сборка контейнера backend
11. [settings.py](./backend/lyalya/lyalya/settings.py) — настройки по запуску основного backend
12. [settings_test.py.py](./backend/lyalya/lyalya/settings_test.py) — настройки по запуску тестового backend
13. [init.sql](./backend/app/config/init.sql) — инициализация базы данных при первом запуске системы
14. [github-actions-market-pulse.yml](./.github/workflows/github-actions-market-pulse.yml) — описание действий для автоматического развертывания системы на сервере (CI-CD)

#### Постоянная оптимизация производительности

Проект на постоянной основе развивается и улучшается, чтоб отслеживать оперативно предстоящие изменения и релизы предлагаем канал связи с командой [@deltams4](https://t.me/deltams4)

## Локальный запуск проекта

### Запуск проекта в docker

1. Скачать проект.
2. В директории с docker-compose.yml запустить команды:

Для сборки проекта:

```bash
sudo docker compose up --build
```

Для сборки проекта в фоновом режиме:

```bash
sudo docker compose up --build -d
```

Если требуется остановить предыдущую версию проекта и удалить собранные образы (данные в volumes сохраняются):

```bash
sudo docker compose down
```

Если требуется остановить предыдущую версию проекта и удалить все сохраняемые данные:

```bash
sudo docker compose down -v
```

Для локального тестирования без `postgreSQL` необходимо изменить [`settings.py`](./backend/lyalya/lyalya/settings.py)
Который расположен по пути [`./backend/lyalya/lyalya/settings.py`](./backend/lyalya/lyalya/settings.py)

### Шаги по миграции

1. Если остались прошлые файлы - удалить их, кроме `__init__.py`
2. Выполнить команду `python manage.py makemigrations`, можно добавить флаг `--settings=lyalya.settings_test` для тестовой БД
3. Выполнить команду `python manage.py migrate`, можно добавить флаг `--settings=lyalya.settings_test` для тестовой БД
4. Запустить тесты `python manage.py test core.tests.models`, можно добавить флаг `--settings=lyalya.settings_test` для тестовой БД, если все тесты пройдены успешно - миграция выполнена успешно

### Локальный запуск frontend

1. В директории [frontend](./frontend) выполнить установку зависимостей `npm install`
2. Из директории [frontend](./frontend) выполнить команду `npm run dev`

### Локальный запуск проекта в docker swarm

Создание на сервере docker swarm manager (используется для бесшовного запуска)

Вызвать команду по первоначальному созданию swarm manager:

```bash
sudo docker swarm init
```

Преобразуем файл docker-compose для работы в docker swarm:

```bash
sudo docker stack deploy --compose-file docker-compose.yml myapp
```

`myapp` - это имя стека, по которому можно проводить запросы и манипуляции
Для просмотра запущенного сервиса можно воспользоваться следующим:

```bash
sudo docker service ls
```

Ручной перезапуск сервиса:

```bash
sudo docker service update --force name_service
```

Перезапуск docker swarm при возникновении ошибок:

```bash
sudo docker swarm leave --force
sudo rm -rf /var/lib/docker/swarm/*
sudo docker swarm init
```

### Запуск тестов

Для запуска тестов backend-части приложения необходимо перейти в директорию [backend](./backend), где находится файл `manage.py`.

#### Основные команды запуска тестов

**Запуск всех тестов проекта:**

```bash
python manage.py test --settings=lyalya.settings_test core.tests
```

**Запуск конкретного модуля:**

```bash
python manage.py test --settings=lyalya.settings_test core.tests.<test_module_name>
```

Пример запуска тестов из модуля `api_view`:

```bash
python manage.py test --settings=lyalya.settings_test core.tests.api_view
```

**Запуск отдельного файла из модуля:**

```bash
python manage.py test --settings=lyalya.settings_test core.tests.<test_module_name>.<filename>
```

Пример запуска тестов из файла `test_product.py` модуля `models`:

```bash
python manage.py test --settings=lyalya.settings_test core.tests.models.test_product
```

#### Команды для проверки покрытия кода

В файле `.coveragerc` указаны файлы-исключения для `coverage` (системные файлы, автоматически генерируемый код и т.п.), чтобы оценка покрытия происходила только по интересующим модулям.

Для проверки покрытия тестами нужно использовать следующую команду с указанием пути до исходного файла:

```bash
coverage run --source=core.<module_name> manage.py test --settings=lyalya.settings_test core.tests.<test_module_name>
```

Пример замера покрытия для `models`:

```bash
coverage run --source=core.models manage.py test --settings=lyalya.settings_test core.tests.models
```

##### Замер покрытия для всех файлов (кроме исключений)

```bash
coverage run manage.py test --settings=lyalya.settings_test core.tests
```

##### Отображение параметров покрытия в консоли

```bash
coverage report
```

##### Отображения параметров покрытия в виде html-отчета

```bash
coverage html
```
